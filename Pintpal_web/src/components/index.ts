/**
 * index.ts (components)
 *
 * Purpose: Barrel for shared UI components used by three or more features.
 * Connects to: features/* pages and components. Stage 0 has no shared widgets yet.
 * Notes: Keep feature-local components inside features/<name>/components instead.
 */

export {};
