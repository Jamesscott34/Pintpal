/**
 * index.ts
 *
 * Purpose: Stage 0 components placeholder for the profile feature (web).
 * Connects to: features/profile pages composed from app/ routes.
 * Notes: Feature-local components only; shared widgets go in src/components.
 */

export {};
