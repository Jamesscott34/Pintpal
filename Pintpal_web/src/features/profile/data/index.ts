/**
 * index.ts
 *
 * Purpose: Stage 0 data-layer placeholder for the profile feature (web).
 * Connects to: Firestore/Auth via utilities/firebase.ts. Used by features/profile/hooks.
 * Notes: No live queries yet in Stage 0.
 */

export {};
